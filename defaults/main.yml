# Standards: 0.1
---
# @var gimp_user:description: User to run user-specific commands
# @var gimp_user:value: gimp_user | default(homebrew_user) | default(ansible_user_id)
# gimp_user:

# @var gimp_started:description: Restart app if already running
gimp_started: True

...
